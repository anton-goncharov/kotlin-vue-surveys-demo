// import { userService } from '@/services';

const state = {
    all: {}
};

const actions = {
}

const mutations = {
}

export const users = {
    namespaced: true,
    state,
    actions,
    mutations
};